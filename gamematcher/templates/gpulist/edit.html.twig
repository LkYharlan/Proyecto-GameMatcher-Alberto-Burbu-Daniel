{% extends 'base.html.twig' %}

{% block title %}Edit Gpulist{% endblock %}

{% block body %}
    <h1>Edit Gpulist</h1>

    {{ include('gpulist/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_gpulist_index') }}">back to list</a>

    {{ include('gpulist/_delete_form.html.twig') }}
{% endblock %}
